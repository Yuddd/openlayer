/** set map
 * para
 * var mapSettiings = $.extend({
        mapSouce: new ol.source.OSM(),
        mapCenter: [0, 0],
        mapZoom: 2
    }, mapOptions);
 **/
var mapOptions = {
    mapTarget: 'map'
}
